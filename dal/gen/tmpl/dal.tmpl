package dao

import "database/sql"

type DAL struct {
  {{range .pascalCaseObjectList -}}
  {{.}} *{{.}}DAO
  {{ end }}
}

func NewDAL(db *sql.DB) *DAL {
	return &DAL{
    {{range .pascalCaseObjectList -}}
    {{.}}: New{{.}}DAO(db),
    {{ end }}
	}
}
